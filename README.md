
# Лабораторная работа №4  
## Написание Unit-тестов (Python, модуль `unittest`)

Данная лабораторная работа посвящена созданию и выполнению модульных (unit) тестов в языке Python с использованием стандартного модуля `unittest`.  
В ходе работы необходимо написать небольшую программу, разработать для неё набор тестов, выполнить их и подготовить отчет.

---

## 📌 Цель работы
- Ознакомиться с принципами модульного тестирования.
- Научиться применять фреймворк `unittest` в Python.
- Научиться выделять функциональные части программы и проверять их корректность автоматически.
- Сформировать навык создания набора тестов и анализа результатов их выполнения.

---

## 📌 Описание тестируемого продукта
В качестве тестируемого продукта используется простой модуль **rectangle.py**, содержащий функции:

```python
def area(a, b):
    return a * b

def perimeter(a, b):
    return 2 * (a + b)

